/** Stub for `three/tsl` (see three-webgpu.ts). Every symbol three-globe references is exported as a never-called placeholder. */
const never = (): never => {
  throw new Error("three/tsl is not bundled in The Network");
};
export const Fn = never;
export const If = never;
export const Loop = never;
export const asin = never;
export const cos = never;
export const exp = never;
export const float = never;
export const instanceIndex = never;
export const negate = never;
export const sin = never;
export const sqrt = never;
export const storage = never;
export const uniform = never;
